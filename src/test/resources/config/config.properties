url = https://www.amazon.com/
browser = Chrome
username =
password =
dbUrl =
dbUserName =
dbPassword =
#if true, the browser doesn't open but everything happens behind the scenes (test front end but browser doesn't open)
#if false, the browser opens
headless = true
#if true, browser doesn't open at all (won't be able to test a front end)
# if false browser opens
APITesting = false